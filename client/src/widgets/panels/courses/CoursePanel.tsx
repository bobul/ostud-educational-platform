import {OstudPanel} from "../../../shared";

export function CoursePanel() {
    return (
        <div>
            <OstudPanel title='Ваші курси: ' type='course' items={} action={} />
        </div>
    );
}